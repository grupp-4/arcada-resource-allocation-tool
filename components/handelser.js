import styles from "./styles.scss"

function HandelserComponent() {
    return (
        <div className={`${styles.gridStyles} ${styles.gridHandelser}`}>
            This is the handelser
        </div>
    )
}

export default HandelserComponent